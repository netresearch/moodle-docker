# Environment configuration (contains secrets)
.env
.envrc.local

# Moodle code (managed via git clone separately)
/moodle/

# Moodle data directory (runtime data)
/moodledata/

# Backups (if created locally)
backups/

# Logs
*.log

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Docker volumes (managed by Docker)
# Not needed in .gitignore as they're not in the project directory

# Temporary files
tmp/
temp/
*.tmp

# SSL certificates (use your own for production)
docker/nginx/ssl/*.pem
